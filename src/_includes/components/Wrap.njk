{#
  Wrap component
  ---
  CMS _data:
    -
  ---
  args:
    flavour=''
      Optional
      String
    flows=false
      Boolean
  ---

  look in Wrap.css fopr all flavours but currently one of:
    content
    columns
    readable
  or nothing for maximum site width

  references:
  role="img" : https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Roles/Role_Img
  fallbacks: https://css-tricks.com/a-complete-guide-to-svg-fallbacks/#fallback-inline-svg-imgtag
#}

{% macro Wrap(
  flavour='',
  flows=false
) %}
{% set content = caller() %} {# ref: https://github.com/mozilla/nunjucks/issues/664 #}
<div class="Wrap{{ ' Wrap--' if flavour }}{{ flavour }}{{ ' u-flows' if flows }}">
  {{ content }}
</div>
{% endmacro %}
