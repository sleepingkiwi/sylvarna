{% extends 'layouts/base.njk' %}

{% block content %}
  <main id="main-content" class="site-main site-main--bespoke" tabindex="-1">
    {{ content | safe }}

    <div class="Wrap Wrap--readable">
      <pre>
<strong>title:</strong>

{{title | safe}}

---

<strong>hero:</strong>

{{hero | dump | safe | replace('":"', ' : ') | replace(',"', '<br><br>') | replace('"', "") | replace('{', '<div style="padding-left:24px;">') | replace('}', '</div>')}}

---
<strong>materialInformation:</strong>

{{materialInformation | dump | safe | replace('":"', ' : ') | replace(',"', '<br><br>') | replace('"', "") | replace('{', '<div style="padding-left:24px;">') | replace('}', '</div>')}}

---

<strong>genericContentBlocks:</strong>

{{genericContentBlocks | dump | safe | replace('":"', ' : ') | replace(',"', '<br><br>') | replace('"', "") | replace('{', '<div style="padding-left:24px;">') | replace('}', '</div>')}}

---

<strong>kitchensTitle:</strong>

{{kitchensTitle | safe}}

---

<strong>kitchensSubTitle:</strong>

{{kitchensSubTitle | safe}}

---

<strong>transparentHeader:</strong>

{{transparentHeader | safe}}

---

<strong>footerPartners:</strong>

{{footerPartners | safe}}

---

<strong>footerCTA:</strong>

{{footerCTA | safe}}
      </pre>
    </div>
  </main>
{% endblock %}
